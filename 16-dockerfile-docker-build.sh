# untuk membuat image dari dockerfile bisa menggunakan perintak docker build
docker build -t nikolas/app:1.0.0 folder-dockerfile
docker build -t nikolas/app:1.0.0 -t nikolas/app:latest folder-dockerfile # membuat nama lebih dari satu